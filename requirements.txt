# SPOCK Dependencies
# Note: We recommend using `uv` for environment management.
# Run `uv sync` instead of `pip install -r requirements.txt`
#
# For uv installation:
#   Windows: powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
#   macOS/Linux: curl -LsSf https://astral.sh/uv/install.sh | sh
#
# Then run: uv sync

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Visualization and utilities
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0
pandas>=1.3.0

# Configuration
pyyaml>=5.4.0

# Optional: PyTorch (for GPU acceleration)
# torch>=1.10.0

# Optional: FAISS for fast ANN
# faiss-cpu>=1.7.0

# Optional: Optimal Transport library
# pot>=0.8.0
